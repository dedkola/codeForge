# code-server password — change before deploying!
# Generate with: echo -n "yourpassword" | base64
apiVersion: v1
kind: Secret
metadata:
  name: code-server-secret
  namespace: codelearn
type: Opaque
data:
  # base64 of "changeme" — REPLACE THIS
  password: Y2hhbmdlbWU=
